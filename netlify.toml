[build]
  command = "echo 'Build complete'"
  publish = "."

[context.production]
  environment = { NODE_ENV = "production" }

# Redirect para capturar el POST del formulario
[[redirects]]
  from = "/"
  to = "/"
  status = 200
  force = false
